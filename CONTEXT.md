# 项目领域上下文

本文件为需要理解"小白养基"业务语义的 Agent 技能提供领域知识。

## 项目简介

"小白养基" 是一个基金和股票投资管理应用：
- **基金持仓管理**：通过蚂蚁基金 App 截图 OCR 导入持仓，跟踪收益
- **股票持仓管理**：OCR 导入或手动录入股票持仓
- **收益统计**：每日归档、盈亏日历、首页 summary 仪表盘
- **盘中估值**：实时抓取东方财富基金估值，提供临时参考
- **行业板块资金参考**：仅展示股票行业/概念板块的公开行情强弱，不推导单只场外基金申赎

技术栈：
- 后端：ASP.NET Core 8.0 (C#)，MySQL + Redis
- 小程序正式前端：`miniprogram/src/`（uni-app / Vue 3 / TypeScript）
- WebApp 正式前端：`wwwroot/index.html`（单文件）

`wwwroot/v2/` 已删除且不再使用；`frontend/src/` 不是正式前端入口。前端修改只能落在上述两个正式入口。

两端正式主题只保留：默认深色“曜石流光”和浅色“雾光银蓝”。历史主题值只做兼容映射，不再作为独立主题展示。

两端布局硬约束：金额与市场状态必须位于独立可收缩区域，任何状态条不得占用金额的可用宽度；WebApp 固定底栏必须保留安全区和足够的页面底部滚动空间，不能遮住页面最后的可操作内容；小程序固定底栏、页头、日历工具栏、首页操作区和弹窗都必须支持窄屏、系统字体放大和安全区。正式发布前至少检查 390、768、1365、2048 四档 WebApp 宽度，以及小程序正式构建。实现和验收记录见 `docs/adr/0003-frontend-cache-and-cdn-policy.md`、`docs/reviews/2026-07-11-responsive-layout-audit.md` 与 `docs/reviews/2026-07-11-cdn-parity-and-dashboard-resilience.md`。

## 白毛股神推文

- “推文”是底部导航独立第 5 个板块，与持仓、板块、资讯、盈亏并列，不得放在持仓页底部。
- 固定目标账号为 `@aleabitoreddit`，列表按 `createdAt` 降序，最多展示最近 20 条。
- 中文翻译优先显示，英文原文保留；翻译失败或未配置 provider 时继续显示英文原文。
- 推文回复随前 20 条展示范围写入 JSON 缓存；WebApp 点击“回”或“查看回复”进入详情页，展示推文与回复的中文译文、英文原文和原文链接。
- 翻译字段和翻译结果随推文与回复写入 JSON 缓存，已有成功译文不得在每次页面打开时重复翻译。
- sidecar 抓取失败保留旧缓存；缓存缺失、抓取失败或翻译失败不得影响基金首页、收益计算、OCR、`DailyArchive`、盈亏日历或首页 summary。
- 数据流与敏感信息边界见 `docs/adr/0005-influencer-posts-source-and-cache.md`。

## 核心领域实体

| 实体 | 说明 |
|------|------|
| `MyFundConfig` | 用户基金配置，含蚂蚁确认金额、昨日收益、持有收益等字段 |
| `FundData` | 基金净值历史（东方财富抓取） |
| `DailyArchive` | 每日收益归档，正式历史档案 |
| `FundValuationEstimate` | 盘中估值，临时数据，不得进入正式日历 |
| `FundTradeOrder` | 基金交易订单 |
| `StockHolding` | 股票持仓 |
| `StockWatchItem` | 股票自选 |
| `StockOcrImportBatch` | 股票 OCR 批量导入批次 |
| `StockOcrImportItem` | 股票 OCR 批量导入明细 |
| `FundValuationCalibration` | 盘后校准样本：真实净值涨跌幅 vs 盘中估算涨跌幅误差，含滚动修正值 |
| `OcrCorrection` | OCR 基金名称修正映射（OCR 名称 → 正确基金代码/名称） |
| `UserInsightSnapshot` | 产品化面板快照（战报、回本榜、赛道暴露等），可跨设备同步 |
| `UserUiState` | 用户 UI 偏好持久化（隐私模式、盈亏页视图、回本模拟默认金额等） |
| `User` | 用户，认证通过 `username` 参数（无 JWT） |

## 关键服务层

| 服务 | 说明 |
|------|------|
| `PortfolioAccounting` | 收益汇总计算核心：确认持仓金额、昨日收益、持有收益/率、盘中估算收益/率等；展示和归档金额为 2 位小数，连续滚动的持仓/收益底稿保留 4 位小数 |
| `PortfolioSettlementService` | 持仓结算：处理待确认买入的生效日期判断和状态流转 |
| `FundTradeTiming` | 加减仓时间估算：15:00 截止、国内交易日顺延；特殊基金日期仅作预计，平台日期优先 |
| `MarketCalendar` | A 股 + 港股休市日历（硬编码年度数据），控制结算和估值抓取在交易日执行 |
| `DailySettlementService` | 按北京时间归档：蚂蚁 OCR 确认 → 正式档案；缺少 OCR → 官方净值 pending 档案 |
| `FundScraperService` | 每 60 秒抓取东方财富基金盘中估值 |
| `NavSettlementService` | 17:00-02:00 期间每 5 分钟执行 T+1 净值结算 |

## 行业板块资金边界

- 单只基金资金流功能已取消：普通场外基金没有可核验的实时申购/赎回人数或净金额公开源；原 `/api/fund/fund-flow` 及两端展示均已移除。
- `/api/fund/capital-flow` 只表示股票行业/概念板块公开行情中的主力资金强弱，不表示基金申赎。
- WebApp 板块基金弹窗保留基金涨幅和近一月涨幅，不再显示或请求单基金资金流。
- 板块强弱只可作为行情热度参考，不得写入持仓、收益、收益日历，也不得表述为确定性预测或直接加减仓建议。

## 加减仓时间口径

- 普通开放式基金：交易日 15:00 前按当日 T 日估算，15:00 后或非交易日顺延到下一个国内交易日；通常 T+1 确认。
- QDII、FOF 和其他特殊基金的确认周期、开放日可能因产品合同不同而变化。代码中的 T+2/T+3 只能标记为“预计”，不得当成平台确认事实。
- OCR 或交易记录明确给出交易日、确认日、可查看收益日时，平台字段优先覆盖名称推算。
- 买入待确认期间，金额可进入平台当前金额，但待确认本金和待确认份额不参加正式确认收益；交易日官方净值到达后可推导待确认份额，确认日再转为有效份额。
- 手工加仓提交后必须同时保留三层口径：`当前展示金额 = 原确认金额 + 当日收益 + 待确认买入`、`今日收益基数 = 原确认金额`、`正式归档金额 = 原确认金额 + 已确认收益`。待确认买入只能出现在第一层，确认前不得进入今日收益率分母或 `DailyArchive`。
- 赎回提交但金额未确认时，不提前扣减已确认份额、金额和成本；平台确认实际到账金额后，使用原交易日和相同赎回份额完成一次结转。
- 旧 `settle-nightly` 路由已删除，旧 `auto-settle` 盘中估值清算入口已退出路由；正式清算只接受官方净值或蚂蚁 OCR 确认数据。

## 基金金额口径强约束

以下规则是硬约束，任何 Agent 修改涉及金额/收益的代码时必须遵守：

1. **蚂蚁基金 App 当前金额** 是平台当前持仓金额的最高优先校准来源，不等于基金公司/销售机构已经确认的正式持仓金额。
2. 当前金额与确认持仓金额必须拆开：`平台当前金额 = 已确认持仓金额 + 买入待确认金额`。
3. **蚂蚁 OCR 快照是截图展示校准源**。当日有蚂蚁 OCR 快照时，首页当前展示以 OCR 快照为准；无新鲜 OCR 时，首页当前金额必须随官方净值或盘中估值的单基金额滚动；正式历史档案只能写入已确认持仓金额和已确认收益。
4. **昨日收益** 只能用于以下两个场景：
   - "昨日确认收益" 展示
   - "盈亏日历" 当日收益记录
5. **禁止** 用 `当前金额 - 昨日收益` 反推蚂蚁确认金额。
6. **禁止** 用 `持有收益 - 昨日收益` 反推蚂蚁持有收益。
7. **盘中估值** 只能作为临时估算，不能写入正式历史日历。
8. 常规持仓截图不要求用户手工核对份额或成本价。截图未提供份额/成本价时，系统可以用官方净值从蚂蚁当前金额自动反推份额，并用 `当前金额 - 持有收益` 推导成本金额；这些派生值只用于后续滚动估值，不能反过来覆盖同日蚂蚁 OCR 的金额、昨日收益或持有收益。资产详情页若 OCR 到 `持仓成本价` 和 `持有份额`，则优先用 `成本价 × 份额` 校准成本金额，并用 `蚂蚁持有收益 = 当前金额 - 成本金额 + 已实现收益` 校准 `RealizedProfit`。
   - `HoldSharesAreConfirmed` / `CostAmountIsConfirmed` 与对应 `Source` 字段持久化记录份额、成本来源。
   - 资产详情页或用户手工确认值优先级最高；普通持仓 OCR 的净值/持有收益反推值不得覆盖确认值。
   - 买入金额和成交净值推导出的份额、成本继续标记为交易派生值；确认前不参加收益，确认后可滚动使用，后续只有资产详情页或用户手工确认值可以覆盖。
9. 官方净值清算后的展示金额按 `上一有效金额 + 已确认收益` 滚动；`份额 × 今日净值` 只用于辅助推算确认收益，不得直接覆盖展示金额，以避免自动反推份额带来的分位误差。
10. 持仓展示金额保持 2 位小数；无新鲜 OCR 时用于连续滚动的持仓金额和结算收益底稿保留 4 位小数，避免每天在展示边界提前吃掉尾数。

依据：
- 中国证监会深圳监管局《持有时间需算清，避免高额手续费》：https://www.csrc.gov.cn/shenzhen/c105614/c7602071/content.shtml
- 《中华人民共和国证券投资基金法》第六十七条：申购在登记机构确认份额时生效，赎回在登记机构确认时生效：https://www.csrc.gov.cn/csrc/c101939/c1045353/content.shtml
- 华夏基金“申购后什么时候能查份额”：普通基金与 QDII/FOF 查询时间存在产品差异：https://m.chinaamc.com/cjwt/gmwt/5501300.shtml

## 盈亏日历口径

- `DailyArchive` 是历史收益的正式档案表。
- 正式历史档案优先来自蚂蚁 OCR 确认数据。
- 缺少蚂蚁确认时，官方净值可以作为 `official-nav-pending` 待确认档案。
- 盘中估值 **不得** 进入正式日历。
- `DailyArchive` 中 `FundCode = 'TOTAL'` 的汇总行是首页"昨日确认收益"的优先读取来源。

## 首页 summary 口径

首页仪表盘展示以下关键数字，计算公式如下：

| 字段 | 计算方式 |
|------|---------|
| 账户总金额 | `OcrSnapshotDate` 与北京时间当前自然日相同时使用蚂蚁当前金额合计，不再叠加盘中估算或官方净值收益；跨过 00:00 后旧 OCR 不再作为当前展示快照，改随最新确认档案、官方净值或盘中估值滚动（包含买入待确认金额） |
| 确认持仓金额 | 平台当前金额 - 买入待确认金额；官方净值确认后必须随单基确认金额滚动 |
| 买入待确认金额 | 蚂蚁 App 显示或用户确认的买入待确认金额 |
| 昨日确认收益 | 最新 DailyArchive TOTAL 行的 DailyProfit |
| 昨日确认日期 | 最新 DailyArchive TOTAL 行的 RecordDate |
| 蚂蚁持有收益 | 有新鲜蚂蚁 OCR 时使用蚂蚁 App 当前持有收益合计；无新鲜 OCR 时，随当前金额和成本滚动计算 |
| 份额和成本金额 | 资产详情页显示成本价/份额时优先按平台字段校准；常规 OCR 截图未显示时自动推导：份额按蚂蚁当前金额 / 官方净值校准，成本金额按当前金额 - 蚂蚁持有收益校准 |
| 份额/成本来源 | 平台详情页或用户手工确认值永久高于普通 OCR 反推值；买入成交净值/金额形成的交易派生值也不被普通 OCR 的两位金额反推覆盖 |
| 官方净值清算金额 | 上一有效金额 + 官方净值确认收益；不用份额 × 今日净值直接覆盖展示金额 |
| 今日盘中估算 | 盘中估算市值 - 确认持仓金额 |
| 今日估算收益率 | 今日盘中估算 / 确认持仓金额 × 100 |

### 今日收益的自然日边界

- 自然日和有效交易日是两个字段。周末、节假日和交易日 09:30 前，有效交易日可以回退到上一交易日，以承接金额、成本和累计收益。
- 一旦北京时间跨过 00:00，当前自然日的今日收益、今日收益率和单基金今日涨跌幅重新从 0 开始；不得把上一交易日 `DailyArchive` 的 `DailyProfit`/`DailyRate` 当成新自然日今日值。
- 交易日 09:30 后才进入当天盘中状态；没有当天估值时仍显示 0 或等待数据，不能回填上一交易日涨跌幅。
- “昨日确认收益”继续读取最新可用 `DailyArchive TOTAL` 行，并同时展示其 `RecordDate`，它与“今日收益”是不同口径。

## 收益率计算口径

```
持仓本金 = 确认持仓金额 - 持有收益
持有收益率 = 持有收益 / 持仓本金 × 100%
今日收益率 = 今日收益 / 昨日基准金额 × 100%
```

待确认买入验算示例：原确认金额 `35000.00` 元，当日涨跌 `-3.88%`，同日 15:00 前买入待确认 `10000.00` 元，则今日收益为 `35000.00 × -3.88% = -1358.00` 元，当前展示金额为 `35000.00 - 1358.00 + 10000.00 = 43642.00` 元。今日收益率分母仍是 `35000.00` 元，待确认的 `10000.00` 元不参与当天收益。

**禁止**为了让页面数字看起来接近而修改上述公式。如果数字对不上，先排查数据来源，不要改公式。

## 领域术语

| 术语 | 含义 |
|------|------|
| 蚂蚁确认数据 | 蚂蚁基金 App 截图经 OCR 提取后确认的数据 |
| 蚂蚁当前金额 | 蚂蚁 App 显示的当前持有金额 |
| 蚂蚁持有收益 | 蚂蚁 App 显示的累计持有收益 |
| 昨日收益 | 蚂蚁 App 显示的前一日确认收益 |
| 盘中估值 | 东方财富 API 返回的实时基金估值（非确认数据） |
| 官方净值 | 基金公司公布的正式净值（T+1 确认） |
| DailyArchive | 每日收益归档表，盈亏日历的数据源 |
| TOTAL 汇总行 | DailyArchive 中 FundCode='TOTAL' 的用户级汇总行 |
