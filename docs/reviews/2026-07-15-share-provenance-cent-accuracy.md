# 2026-07-15 份额来源与分钱精度复盘

## 结论

2026-07-15 蚂蚁基金截图总金额为 `98805.42`，生产库六只基金两位金额合计为 `98805.40`。差额不是总额汇总时才发生的四舍五入，也不是 2026-07-13 的 `10000.00` 元加仓参与了错误收益；根因是两只旧基金的资产详情页平台份额曾被普通持仓 OCR 的金额/净值反推份额覆盖，各产生 `0.01` 元分位差。

事实来源：用户提供的 2026-07-15 蚂蚁基金持仓截图；2026-07-15 08:21 CST 对生产库 `MyFunds`、`DailyArchives` 和官方净值记录的只读核对。

## 精确验算

### 023763

- 数据库旧反推份额：`26756.274256`
- 2026-07-14 官方净值：`0.7665`
- 旧计算：`26756.274256 × 0.7665 = 20508.684217...`，展示为 `20508.68`
- 蚂蚁资产详情页份额：`26756.28`
- 平台份额计算：`26756.28 × 0.7665 = 20508.688620`，展示为 `20508.69`

差额：`+0.01`。

### 008282

- 数据库旧反推份额：`6.009846`
- 2026-07-14 官方净值：`3.1323`
- 旧计算：`6.009846 × 3.1323 = 18.824640...`，展示为 `18.82`
- 蚂蚁资产详情页份额：`6.01`
- 平台份额计算：`6.01 × 3.1323 = 18.825123`，展示为 `18.83`

差额：`+0.01`。两只基金合计正好补齐总额 `0.02`。

### 017968 的 10000 元买入

- 买入前份额：`23005.1006`
- 买入金额/成交净值：`10000.00 / 1.4375 = 6956.5217` 份（按系统 4 位交易份额底稿）
- 买入后份额：`29961.6223`
- 2026-07-14 官方净值：`1.4549`
- 验算：`29961.6223 × 1.4549 = 43591.164284...`，展示为 `43591.16`

该结果与蚂蚁截图一致，因此本次 `0.02` 元差异不是这笔买入导致。

## 修复口径

1. `MyFundConfig` 持久化份额、成本各自的确认状态和来源。
2. 资产详情页和用户手工确认值优先级最高，普通 OCR 派生值不得覆盖。
3. 买入金额与成交净值形成的交易派生份额、成本不参与确认前收益，也不得被普通 OCR 的两位金额反推覆盖。
4. 平台精确值仍用于后续估值辅助；当日金额、昨日收益和持有收益继续以蚂蚁 OCR 快照为最高展示校准源。

## 回归覆盖

- 普通 OCR 派生份额可初始化空字段。
- 资产详情页精确份额可覆盖派生份额。
- 精确份额写入后，后续普通 OCR 不可降级覆盖。
- 买入成交净值派生份额不被普通 OCR 覆盖。
- 资产详情页精确成本写入后，后续普通 OCR 不可降级覆盖。
- 2026-07-13 的待确认 `10000.00` 元继续不进入当日收益基数和正式归档。

## 生产校准结果

- `023763`：份额 `26756.28`，2026-07-14 金额 `20508.69`，持有收益 `-491.31`。
- `008282`：份额 `6.01`，2026-07-14 金额 `18.83`，持有收益 `9.80`。
- 2026-07-14 `DailyArchive TOTAL`：金额 `98805.42`，当日收益 `593.69`，持有收益 `-11898.78`。

2026-07-15 08:51 CST 已完成以下操作：

1. GitHub Actions 后端部署 `29379922399` 成功，启动迁移已创建份额/成本来源字段。
2. 修改前备份已写入服务器 `/root/db-backups/xiaobaiyangji/`；文件名分别为 `myfunds_dabai521_before_cent_fix_20260715_085102.sql` 和 `dailyarchives_20260714_before_cent_fix_20260715_085102.sql`，两份备份均非空且包含 INSERT 数据。
3. 六只当前持仓的数据库合计复核为：金额 `98805.42`、昨日收益 `593.69`、持有收益 `-11898.78`。
4. 2026-07-14 六只基金档案及 `TOTAL` 均标记为 `alipay-confirmed` / `alipay-confirmed-total`，`IsFinal=1`。
5. 服务重启后 `guzhi-assistant.service` 为 `active`，本机 `/api/health` 返回 `success=true`。

## 19:33 语义纠正

生产校准时曾把 `008282` 的 `0.97` 元口径差写入 `RealizedProfit`，导致前端错误显示“已落袋”。用户确认该基金没有卖出，因此该写入不成立，现纠正为：

- `RealizedProfit = 0.00`，只表示确认卖出收益。
- `PlatformHoldingAdjustment = 0.97`，计算式为 `9.80 - (18.83 - 10.00 + 0.00)`，只用于让无新鲜 OCR 时的持有收益继续与平台 `9.80` 对齐。
- `0.97` 的具体经济来源无法仅凭当前截图确认，标记为“待核实”；不得称为卖出、分红或落袋收益。

事实来源：用户提供的 2026-07-15 蚂蚁资产详情截图；2026-07-15 19:31 CST 对生产库 `MyFunds` 的核对与纠正。

### 生产验证

1. GitHub 提交 `c0ade90` 已推送到 `master`，后端部署 `29413675488` 成功。
2. 启动迁移 `202607150002_AddPlatformHoldingAdjustment` 已写入生产库迁移历史。
3. 修改前单基金备份为 `/root/db-backups/xiaobaiyangji/myfund_008282_before_adjustment_split_20260715_200348.sql`，文件大小 `1766` 字节。
4. 生产库最终值：当前金额 `18.83`、成本金额 `10.00`、`RealizedProfit=0.00`、`PlatformHoldingAdjustment=0.97`、OCR 持有收益 `9.80`。
5. 手工验算：`18.83 - 10.00 + 0.00 + 0.97 = 9.80`。服务重启后 `guzhi-assistant.service` 为 `active`，本机 `/api/health` 返回 `success=true`。

## 23:01 正式净值更新后的金额漂移

### 现象与来源

用户 2026-07-15 23:01 CST 提供的蚂蚁基金截图显示：总金额 `99035.59`、昨日收益 `593.69`、持有收益 `-11668.61`。同日晚间对生产 `/api/Fund/today?username=dabai521&force=true` 的鉴权只读核对显示：WebApp 总金额 `99462.83`、昨日收益 `593.69`、持有收益 `-11898.78`。

事实来源：用户提供的蚂蚁基金截图；生产 `/api/Fund/today` 与对应 `DailyArchive` 的鉴权只读响应。生产修复与重新部署状态在本地提交前均为“待执行”，不得把本节的本地验证写成已上线。

### 根因验算

六只基金中只有 `012349` 的金额不一致：

- WebApp：`35444.37`
- 蚂蚁基金：`35017.13`
- 差额：`427.24`
- 可靠份额：`54774.17`
- 2026-07-15 官方净值：`0.6393`
- 精确计算：`54774.17 × 0.6393 = 35017.126881`，两位展示为 `35017.13`

因此总额应为：

`43207.65 + 35017.13 + 20738.79 + 51.03 + 17.89 + 3.10 = 99035.59`

持有收益仍冻结在净值更新前 OCR 的 `-11898.78`，而当天六只基金确认收益合计为 `230.17`。正式净值更新后应滚动为：

`-11898.78 + 230.17 = -11668.61`

### 修复边界

1. 定时清算、手动真实净值同步和首页接口统一使用可靠份额异常校验。
2. `份额 × 官方净值` 与滚动金额相差 `<= 0.01` 元时保留平台滚动金额，吸收两位展示分位差。
3. 差额 `> 0.01` 元时判定旧基数或重复结算异常，使用精确金额自愈。
4. 只有平台/手工确认份额和买入成交净值派生份额可以触发自愈；普通持仓 OCR 的金额/净值反推份额不能覆盖平台金额。
5. 正式净值确认后，持有收益按最新金额、成本、真实已实现收益和 `PlatformHoldingAdjustment` 重算；昨日收益及已确认历史档案不被改写。

本地回归期望：总金额 `99035.59`、昨日收益 `593.69`、持有收益 `-11668.61`。
