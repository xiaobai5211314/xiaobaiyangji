# 2026-07-22 工作日志

## 修复 2026-07-20 盈亏日历 -4.32% 数据损坏（用户 dabai521）
- 现象：盈亏日历 07-20 显示 TOTAL 收益率 −4.32%，用户反馈异常。
- 根因：017968（华富科技动能混合C）的 07-20 `DailyArchive` 行被错误写入了 TOTAL 汇总值（assets/profit/rate 与 TOTAL 行字节级相同），导致 TOTAL 行也变成单只基金错误数值。诱因：07-20 当天有一笔约 ¥3,379 的 017968 加仓，且系统当时缺少 07-20 官方净值（外部净值源未返回），结算回退写出损坏数据。
- 验证口径：东方财富真实净值 07-17=1.3021 / 07-20=1.2459 / 07-21=1.3051；07-20→07-21 的 +4.75% 与未损坏的 07-21 归档行 dailyRate 完全吻合，确认来源可靠。
- 修正值（经 `POST /api/fund/save-archive` 写入，source=client-preview）：
  - 017968 07-20：assets 43752.61、dailyProfit −1821.18、dailyRate −4.00%（含加仓成本基数口径）
  - TOTAL 07-20：assets 99725.07、dailyProfit −354.86、dailyRate −0.35%
  - 其余 5 只基金（008282/012349/013273/023763/161226）行未改动。
- 勾稽：TOTAL 07-20 assets 99725.07 + 07-21 dailyProfit 2760.6 ≈ 07-21 TOTAL assets 102485.66，一致。
- `settle-daily?date=2026-07-20` 返回 saved=0（官方 07-20 NAV 不在 FundRecords，无法自愈合），故走 save-archive 手工修正。
- 已更新 CHANGELOG.md（2026-07-22 条目），提交 b124607 并 push 到 master（同批次含板块连续天数 B' 与浅色主题文字对比度修复）。

## 新增 DailyArchive 不变式守卫（根治 07-20 类损坏，commit 8f4534c）
- 代码：`DailyArchiveService.SanitizeArchiveRows`（新增静态方法）—— 多基金组合下，若某基金行 `Assets`≈TOTAL `Assets`（抄写指纹），判定为损坏并剔除，并用剩余有效基金重算 TOTAL（`source=guard-recomputed-total`），保证 TOTAL.Assets≡Σ基金Assets；单基金组合豁免。
- 接入点：`DailySettlementService.BuildArchiveRows`（结算落库前）+ `Controllers/FundController.SaveArchive`（客户端预览落库前）。
- 测试：`tests/PortfolioAccounting.Tests/Program.cs` 新增用例 A（抄写指纹→剔除1行+重算）/ 用例 B（单基金不误伤）。`dotnet build` 0 错误；回归测试全 PASS。
- 线上扫描：对 dabai521 全历史（537 条 `get-archives`）扫描，除已修 07-20 外无同类损坏。

## 安全加固（已完成，commit 7004bdf）
- 守卫下沉：把 `SanitizeArchiveRows` 接入 `FundController.UpsertDailyArchivesAsync`（所有写入的汇聚点），覆盖上次漏网的 `BuildArchiveRowsFromCurrentHoldings` 前台结算路径；`SettleDaily`/`SaveArchive`/OCR 归档均自动净化，零漏网。
- settle-daily 净值缺失显式标记：两处 `BuildArchiveRows*` 在基金官方净值缺失时写 `Source="nav-missing"` 行（不计入 TOTAL），前端当日明细显示「净值缺失」。配套：`SanitizeArchiveRows` 检测条件加 `TOTAL.Assets>0.01` 守护防误删；`UpsertAsync` 允许 nav-missing 行落库（仍拦假 0 数据）。
- 生产 JWT 密钥漏洞已消除：`appsettings.Production.json` 新增 `Auth:TokenSecret` 真实强随机密钥（32 字节 base64），部署后旧占位符 token 失效、需重新登录。密钥明文进 git 历史（项目既有 Production 配置随仓库部署模式），如需更严格可改环境变量注入并滚动。
- 测试：新增用例 C（守卫经写入路径生效）+ D/D2（nav-missing 不误删、TOTAL=0 全缺失不误删）。`dotnet build` 0 错误；测试全 PASS。已提交 7004bdf 并 push 到 master。

## 板块基金雷达定时预热（commit 3884971）
- 需求：用户反馈板块页每次打开慢，希望打开即最新、零等待（像官网一样快）。
- 方案：后端定时预热（用户选 A1 自 ping 零改动）。新增 `SectorRadarWarmupService`（BackgroundService）按交易时段(2min)/非交易时段(30min) 周期自 ping 本进程 `GET {SelfBaseUrl}/api/fund/sectors?force=true`，100% 复用 GetSectors 构建+Redis/内存/DB 写回，零改动控制器。
- 周期纯函数 `SectorRadarScheduleHelper.GetWarmupInterval` 与 `FundController.GetExternalDataFreshTtl` 对齐（可注入时间便于单测）。
- 配置：`appsettings(.Production).json` 写 `SelfBaseUrl=http://127.0.0.1:7084`。**已 grep nginx `proxy_pass http://127.0.0.1:7084` 确认生产 Kestrel 即 HTTP 127.0.0.1:7084**，自 ping 直连生效，无 503 风险（6h 内存 stale `FundSectorRadarV7_Stale` 兜底）。
- `Program.cs`：注册命名 HttpClient `SectorWarmup`(Timeout 180s) + `AddHostedService<SectorRadarWarmupService>()`。
- 测试：回归用例 交易/非交易/周末/边界(11:35 含、12:54 午休)，全 PASS。`dotnet build` 0 错误。
- 已更新 CHANGELOG.md，提交 3884971 并 push 到 master（触发后端 CI 部署）。排除 docs/*、arch_dabai521.json、.workbuddy/ 等环境产物。

## 大盘指数预热 + 资讯移动端遮挡修复（commit e67ce4f）
- 需求：①用户反馈「全球大盘雷达」打开慢（同板块雷达问题）；②资讯面板手机端右侧「资讯影响时间线」被遮挡看不到（PC 正常）。
- 大盘指数预热：复用 `SectorRadarWarmupService`，同一预热周期末尾顺带自 ping `GET {SelfBaseUrl}/api/fund/global-indices?force=true`，复用该端点 Redis(`api:fund:global-indices:v1`)+DB(`global_indices_1y_v2`) 多级缓存重建，`force=true` 确保真重建；同 HttpClient `SectorWarmup`、同容错（200/503/异常）。零新文件，仅改 `Services/SectorRadarWarmupService.cs`。
- 资讯移动端遮挡根因：单文件含 light/dark 两套主题各自 media query 块，原 light 768px 块缺 `.news-page-layout` 单栏覆盖、两套 900px 块均缺失；双栏 Grid `minmax(0,1fr) minmax(280px,.42fr)` 在 light 手机端及横屏/宽屏手机(769–900px) 挤压右栏。改动：light 900px/768px 与 dark 900px 三处补 `.news-page-layout { grid-template-columns: 1fr; }`，基础规则补 `overflow:hidden`。现两套主题 ≤900px/≤768px 均单栏，右栏不再遮挡。
- `dotnet build` 0 错误；回归测试全 PASS（含预热间隔用例）。提交 e67ce4f 并 push 到 master（触发后端+前端 CI）。

## 前端缓存增强：板块 localStorage 顺延 + 资讯页静默刷新（commit 5477578）
- 需求：用户确认「需要」此前提议的前端缓存增强（两项都做）：①延长板块 localStorage 缓存时长；②资讯页开页即显缓存 + 后台静默刷新。
- 纯前端单文件 `wwwroot/index.html` 改动，零后端、零新文件。快速模式：主理人探明现有缓存机制（板块 `sector_fast_cache_v3`/SECTOR_CACHE_DURATION；资讯 `news_cache_v2_*`/NEWS_CACHE_DURATION + `fetchNews` 已有 background 参数）→ 工程师改 → QA 静态复核（无浏览器环境，做分支推演）。
- 板块：`SECTOR_CACHE_DURATION` 180000→600000（内存复用 3→10min）；`sector_fast_cache_v3` 磁盘窗口 `30*60*1000`→`60*60*1000`（30min→1h）。
- 资讯：新增 `fetchNews` 的 `refresh` 选项；开页触发 `fetchNews(newsMode, false, {background:true, refresh:true})`——先 apply 本地旧数据（内存 ≤10min: NEWS_CACHE_DURATION 2→10min；磁盘 ≤30min: 15→30min）再后台 no-store fetch 静默更新；`background` 跳过 spinner 置位，全程不转圈。force 手动刷新/切模式/开面板路径不变；`warmNewsCache()` 仍空操作保首屏速度。
- QA 路由判定 NoOne（通过）。非阻塞边缘建议：首访+服务端失败+无缓存时 background=true 不弹 toast 致空白页，留作后续迭代，本轮不修。
- 提交 5477578 并 push 到 master（前端 CI 部署）。CHANGELOG 同更新。

## 资讯(7x24快讯)后端定时预热，闭环「打开即热」（commit 662802d）
- 需求：用户确认「需要」把 news 也加进后端预热（此前已做 sectors/global-indices 预热 + 前端缓存增强），彻底闭环「永远瞬时」。
- 关键前置：news 端点 `GET /api/fund/news`(`FundController` L9854) 本身无 `[Authorize]`、公开，但 `Program.cs` JWT 免鉴白名单(L159-172) 只含 sectors/global-indices 等、**不含 news**。自 ping（无 token）会掉进 `if(!isPublic)` 分支被 L184-190 返 401。根因确认 + 修复：在白名单加 `path.StartsWith("/api/fund/news")`（L167，与 sectors 并列）。对前端零副作用——控制器无 [Authorize]，带不带 token 返回一致；`isPublic=true` 仅跳过中间件 token 校验。
- `SectorRadarWarmupService.WarmUpOnceAsync` 末尾顺带自 ping `GET {SelfBaseUrl}/api/fund/news?mode=global&force=true`（复用 HttpClient `SectorWarmup`、同容错）。仅预热公开 global 模式（username 空 → 缓存键 `NewsV3_global__False_80`），holding 模式依赖 username 由前端 30min localStorage(`news_cache_v2_{user}_global_0`) 兜底；已登录用户键含 currentUser 不命中后端预热，同样由前端兜底——为预期边界。
- 质量关卡：工程师实现 → 主理人逐行 git diff 发现 L104 `catch(Exception)` 缩进 12 空格瑕疵（C# 不认缩进故 build 仍 0 错误，但脏代码）→ 回派工程师修正为 8 空格（仅格式）→ 复跑 `dotnet build` **0 警告0错误** → QA 静态复核路由判定 NoOne（通过）。`.workbuddy/memory/*` 等环境产物未进提交。
- 提交 662802d 并 push 到 master（后端 CI 部署）。CHANGELOG 同更新。
